.DS_Store

# Lossless masters stay local — the generator publishes compressed .m4a twins
tracks/**/*.wav
tracks/**/*.aif
tracks/**/*.aiff
tracks/**/*.flac

# Trash: deleted songs archived locally until permanently removed via the player
.trash/
