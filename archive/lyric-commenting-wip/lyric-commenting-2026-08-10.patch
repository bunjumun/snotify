diff --git a/music.html b/music.html
index 00213cf..370ed94 100644
--- a/music.html
+++ b/music.html
@@ -218,41 +218,169 @@
   body.authed:not(.singlemix) #refToggle { display: inline-flex; }
   @media (max-width: 620px) { .ref-grid { grid-template-columns: 1fr; } }
 
-  /* ---------- Lyrics ---------- */
-  .lyr-bar { display: flex; align-items: center; gap: 12px; margin-bottom: 10px; }
+  /* ---------- Lyrics ----------
+     Two habits everyone already has from a word processor: comment on a phrase
+     without touching it, or propose the edit itself and let someone accept it.
+     The document sits on the left, the threads about it sit in the margin
+     beside the lines they belong to. */
+  #lyricDrawer.open { max-height: 76vh; }
+  .lyr-bar { display: flex; align-items: center; gap: 10px; margin-bottom: 10px; flex-wrap: wrap; }
   .lyr-drop { position: relative; flex: none; }
   .lyr-drop .vermenu { bottom: auto; top: calc(100% + 6px); left: 0; right: auto; min-width: 240px; }
   .lyr-drop.open .vermenu { display: block; }
-  .lyr-switch { display: inline-flex; align-items: center; gap: 6px; cursor: pointer; color: var(--text-dim); font-size: 12px; }
-  .lyr-switch b { color: var(--text); font-variant-numeric: tabular-nums; }
-  .lyr-body {
-    max-height: 30vh; overflow-y: auto; padding: 2px 0;
-    font-size: 15px; line-height: 1.75; white-space: pre-wrap;
-  }
-  .lyr-line { padding: 0 6px; border-radius: 5px; }
-  .lyr-line.blank { min-height: 10px; }
-  /* A line with alternatives only announces itself in review mode — lyrics
-     should read as lyrics until you go looking. */
-  body.lyr-review .lyr-line.has { background: rgba(255,180,84,.13); cursor: pointer; box-shadow: inset 2px 0 0 var(--warn); }
-  body.lyr-review .lyr-line.has:hover { background: rgba(255,180,84,.22); }
-  /* Something you have staged. Deliberately loud, and only ever visible in
-     review mode, so nobody mistakes a try-on for the band's draft. */
-  .lyr-line.staged { background: rgba(0,208,164,.16); box-shadow: inset 2px 0 0 var(--accent-2); cursor: pointer; }
-  .lyr-star {
-    display: none; margin: 1px 0 1px 4px; color: var(--warn); cursor: pointer;
-    font-size: 12px; letter-spacing: .5px;
-  }
-  body.lyr-review .lyr-star { display: block; }
+  /* The mode selector, in the same place and the same spirit as the one in the
+     top-right of a Google doc: it decides what typing means. */
+  .lyr-modes { display: inline-flex; flex: none; background: var(--bg-row); border: 1px solid var(--line); border-radius: 9px; padding: 2px; }
+  .lyr-modes button {
+    border: none; background: none; color: var(--text-mute); cursor: pointer;
+    font: inherit; font-size: 11.5px; font-weight: 700; letter-spacing: .02em;
+    padding: 5px 11px; border-radius: 7px; display: inline-flex; align-items: center; gap: 5px;
+  }
+  .lyr-modes button:hover { color: var(--text); }
+  .lyr-modes button.on { background: var(--bg-soft); color: var(--text); box-shadow: 0 1px 0 rgba(0,0,0,.4); }
+  .lyr-modes button[data-mode="suggest"].on { color: var(--warn); }
+  .lyr-modes button[data-mode="edit"].on { color: var(--accent); }
+  .lyr-count { color: var(--text-mute); font-size: 11.5px; }
+  .lyr-count b { color: var(--text); font-variant-numeric: tabular-nums; }
+
+  /* Document + margin. Both scroll together so a card never drifts off its line. */
+  .lyr-doc { display: grid; grid-template-columns: minmax(0,1fr) 296px; gap: 16px; max-height: 46vh; overflow-y: auto; padding: 2px 0 8px; }
+  body.lyr-mode-edit .lyr-doc { display: none; }
+  .lyr-text { font-size: 15px; line-height: 1.8; white-space: pre-wrap; min-width: 0; }
+  .lyr-line { padding: 1px 6px; border-radius: 5px; position: relative; }
+  .lyr-line.blank { min-height: 12px; }
+  body.lyr-mode-suggest .lyr-line { cursor: text; }
+  body.lyr-mode-suggest .lyr-line:hover { background: rgba(255,255,255,.035); }
+  .lyr-line.focus { background: rgba(255,180,84,.14); }
+  /* A line with more than one suggested replacement can't show them all at
+     once — it says so, and the cards carry the alternatives. */
+  .lyr-line.multi { text-decoration: underline wavy var(--warn); text-underline-offset: 4px; }
+  .lyr-line .cnt { color: var(--text-mute); font-size: 11px; margin-left: 8px; }
+
+  /* Tracked changes, rendered word by word: what goes in wears the author's
+     colour, what comes out is struck through. */
+  .lyr-text ins { text-decoration: none; color: var(--who, var(--accent-2)); background: color-mix(in srgb, var(--who, #00d0a4) 16%, transparent); border-radius: 3px; padding: 0 1px; }
+  .lyr-text del { color: var(--text-mute); text-decoration: line-through; text-decoration-color: var(--who, var(--danger)); }
+  .lyr-line.staged { box-shadow: inset 2px 0 0 var(--accent-2); }
+  .lyr-line.pending { box-shadow: inset 2px 0 0 var(--who, var(--warn)); }
+  /* The stretch of words a comment was hung on. */
+  .lyr-anchor { background: rgba(255,180,84,.18); box-shadow: inset 0 -2px 0 var(--warn); border-radius: 2px; cursor: pointer; }
+  .lyr-anchor.focus { background: rgba(255,180,84,.34); }
+
+  /* Between two lines: where a suggested new line goes, and where the lines a
+     draft dropped are still findable. */
+  .lyr-gap { display: flex; align-items: center; gap: 8px; min-height: 6px; margin: 0 0 0 4px; }
+  .lyr-gap .plus {
+    display: none; width: 15px; height: 15px; line-height: 13px; text-align: center;
+    border: 1px dashed var(--line); border-radius: 4px; color: var(--text-mute);
+    font-size: 11px; cursor: pointer; flex: none;
+  }
+  body.lyr-mode-suggest .lyr-gap:hover .plus { display: block; }
+  body.lyr-mode-suggest .lyr-gap .plus:hover { color: var(--accent); border-color: var(--accent); border-style: solid; }
+  .lyr-star { color: var(--warn); cursor: pointer; font-size: 12px; }
   .lyr-star:hover { color: var(--text); }
   .lyr-star .cnt { color: var(--text-mute); font-size: 11px; }
+  /* Typing straight into a line: in suggesting mode this makes a suggestion. */
+  .lyr-inline {
+    width: 100%; background: var(--field); border: 1px solid var(--warn); color: var(--text);
+    border-radius: 6px; padding: 2px 6px; font: inherit; font-size: 15px; outline: none;
+  }
+
+  /* ---- the margin ---- */
+  .lyr-rail { position: relative; min-height: 10px; }
+  .lyr-railtabs { display: flex; gap: 4px; margin-bottom: 8px; }
+  .lyr-railtabs button {
+    flex: 1; border: 1px solid var(--line); background: var(--bg-row); color: var(--text-mute);
+    border-radius: 7px; padding: 4px 6px; font: inherit; font-size: 11px; font-weight: 700; cursor: pointer;
+  }
+  .lyr-railtabs button.on { color: var(--text); border-color: var(--accent); }
+  .lyr-cards { position: relative; }
+  .lyr-card {
+    position: absolute; left: 0; right: 0; background: var(--bg-soft);
+    border: 1px solid var(--line); border-left: 3px solid var(--who, var(--line));
+    border-radius: 10px; padding: 9px 11px; font-size: 12.5px;
+    transition: top .16s ease, box-shadow .16s ease;
+  }
+  .lyr-card.focus { box-shadow: var(--shadow); border-color: var(--accent); }
+  .lyr-card.done { opacity: .62; }
+  .lc-head { display: flex; align-items: center; gap: 7px; margin-bottom: 5px; }
+  .lc-av {
+    flex: none; width: 20px; height: 20px; border-radius: 50%; background: var(--who, var(--accent));
+    color: #08080a; font-size: 10px; font-weight: 800; line-height: 20px; text-align: center;
+  }
+  .lc-name { font-weight: 700; color: var(--text); }
+  .lc-when { color: var(--text-mute); font-size: 11px; }
+  .lc-acts { margin-left: auto; display: flex; gap: 3px; flex: none; }
+  .lc-acts span {
+    cursor: pointer; color: var(--text-mute); border: 1px solid transparent;
+    border-radius: 6px; padding: 1px 5px; font-size: 12px; line-height: 16px;
+  }
+  .lc-acts span:hover { color: var(--text); border-color: var(--line); background: var(--bg-row); }
+  .lc-acts span.ok:hover { color: var(--accent-2); border-color: var(--accent-2); }
+  .lc-acts span.no:hover { color: var(--danger); border-color: var(--danger); }
+  .lc-kind { color: var(--text-mute); font-size: 10.5px; text-transform: uppercase; letter-spacing: .07em; font-weight: 700; margin-bottom: 4px; }
+  .lc-quote { color: var(--text-dim); border-left: 2px solid var(--warn); padding-left: 7px; margin-bottom: 5px; font-style: italic; word-wrap: break-word; }
+  .lc-diff { word-wrap: break-word; margin-bottom: 4px; }
+  .lc-diff ins { text-decoration: none; color: var(--who, var(--accent-2)); }
+  .lc-diff del { color: var(--text-mute); text-decoration: line-through; }
+  .lc-text { word-wrap: break-word; }
+  .lc-alt { display: flex; gap: 7px; align-items: flex-start; padding: 5px 6px; margin: 4px -6px 0; border-radius: 7px; }
+  .lc-alt:hover { background: var(--bg-hover); }
+  .lc-alt .k { flex: none; width: 16px; height: 16px; border-radius: 4px; background: var(--bg-row); border: 1px solid var(--line); color: var(--text-dim); text-align: center; font-size: 10px; font-weight: 800; line-height: 15px; }
+  .lc-alt .t { flex: 1; min-width: 0; }
+  .lc-alt .t .w { color: var(--text-mute); font-size: 10.5px; }
+  .lc-assign { margin-top: 5px; color: var(--warn); font-size: 11px; }
+  .lc-assign b { color: var(--text); }
+  .lc-react { display: flex; flex-wrap: wrap; gap: 4px; margin-top: 6px; }
+  .lc-react .r {
+    border: 1px solid var(--line); background: var(--bg-row); border-radius: 20px;
+    padding: 1px 7px; font-size: 11px; cursor: pointer; color: var(--text-dim);
+  }
+  .lc-react .r.mine { border-color: var(--accent); color: var(--text); }
+  .lc-react .r.add { color: var(--text-mute); border-style: dashed; }
+  .lc-picker { display: none; gap: 3px; margin-top: 5px; }
+  .lc-picker.open { display: flex; }
+  .lc-picker span { cursor: pointer; font-size: 14px; padding: 1px 3px; border-radius: 5px; }
+  .lc-picker span:hover { background: var(--bg-hover); }
+  .lc-replies { margin: 6px 0 0; border-top: 1px solid var(--line); padding-top: 6px; }
+  .lc-rep { display: flex; gap: 6px; padding: 3px 0; }
+  .lc-rep .lc-av { width: 16px; height: 16px; line-height: 16px; font-size: 9px; }
+  .lc-rep .b { flex: 1; min-width: 0; word-wrap: break-word; }
+  .lc-rep .b .w { color: var(--text-mute); font-size: 10.5px; }
+  .lc-rep .x { color: var(--text-mute); cursor: pointer; font-size: 11px; }
+  .lc-rep .x:hover { color: var(--danger); }
+  .lc-reply { display: flex; gap: 5px; margin-top: 6px; }
+  .lc-reply input {
+    flex: 1; min-width: 0; background: var(--field); border: 1px solid var(--line); color: var(--text);
+    border-radius: 7px; padding: 5px 8px; font: inherit; font-size: 12px; outline: none;
+  }
+  .lc-reply input:focus { border-color: var(--accent); }
+  .lc-reply button { border: 1px solid var(--line); background: var(--bg-row); color: var(--text-dim); border-radius: 7px; padding: 4px 9px; font: inherit; font-size: 11px; font-weight: 700; cursor: pointer; }
+  .lc-reply button:hover { color: var(--text); border-color: var(--accent); }
+  .lyr-railempty { color: var(--text-mute); font-size: 12px; padding: 6px 2px; }
+
+  /* Highlight something and these two appear over it, the way they do in a doc. */
+  .lyr-seltool {
+    display: none; position: fixed; z-index: 95; gap: 4px;
+    background: var(--bg-soft); border: 1px solid var(--line); border-radius: 9px;
+    box-shadow: var(--shadow); padding: 4px;
+  }
+  .lyr-seltool.open { display: flex; }
+  .lyr-seltool button {
+    border: none; background: none; color: var(--text-dim); cursor: pointer;
+    font: inherit; font-size: 12px; font-weight: 600; padding: 5px 9px; border-radius: 6px;
+  }
+  .lyr-seltool button:hover { background: var(--bg-hover); color: var(--text); }
+
+  /* Writing the words by hand — the third mode, and the only one that can
+     rewrite everything at once. */
   .lyr-edit {
-    display: none; width: 100%; min-height: 26vh; resize: vertical;
+    display: none; width: 100%; min-height: 32vh; resize: vertical;
     background: var(--field); border: 1px solid var(--accent); color: var(--text);
     border-radius: 8px; padding: 10px 12px; font: inherit; font-size: 15px;
     line-height: 1.7; outline: none;
   }
-  body.lyr-editing .lyr-edit { display: block; }
-  body.lyr-editing .lyr-body { display: none; }
+  body.lyr-mode-edit .lyr-edit { display: block; }
   .lyr-stage {
     display: none; align-items: center; gap: 10px; margin-top: 10px;
     padding: 9px 12px; background: var(--bg-row);
@@ -262,15 +390,17 @@
   .lyr-stage .btn { padding: 6px 14px; font-size: 12px; border: none; border-radius: 8px; cursor: pointer; font-weight: 600; }
   .lyr-stage .btn.primary { background: var(--accent); color: #fff; }
   .lyr-stage .btn.ghost { background: var(--bg-soft); color: var(--text-dim); border: 1px solid var(--line); }
-  /* The alternatives popover */
+
+  /* The composer: one box for a new comment or a new suggestion. */
   .lyr-pop {
-    display: none; position: fixed; z-index: 90; width: min(420px, 92vw);
+    display: none; position: fixed; z-index: 96; width: min(420px, 92vw);
     background: var(--bg-soft); border: 1px solid var(--line);
     border-radius: var(--radius); box-shadow: var(--shadow); padding: 12px;
   }
   .lyr-pop.open { display: block; }
   .lyr-pop-head { color: var(--text-mute); font-size: 11px; text-transform: uppercase; letter-spacing: .08em; font-weight: 700; margin-bottom: 8px; }
-  .lyr-pop-list { max-height: 40vh; overflow-y: auto; margin: 0 -4px; }
+  .lyr-pop-quote { color: var(--text-dim); border-left: 2px solid var(--warn); padding-left: 8px; margin-bottom: 8px; font-size: 13px; font-style: italic; max-height: 66px; overflow: hidden; }
+  .lyr-pop-list { max-height: 32vh; overflow-y: auto; margin: 0 -4px 8px; }
   .lyr-opt { display: flex; gap: 9px; padding: 7px 8px; border-radius: 7px; cursor: pointer; }
   .lyr-opt:hover { background: var(--bg-hover); }
   .lyr-opt .key {
@@ -278,30 +408,33 @@
     font-size: 11px; font-weight: 800; line-height: 18px;
     background: var(--bg-row); border: 1px solid var(--line); color: var(--text-dim);
   }
-  .lyr-opt.orig .key { border-color: var(--accent-2); color: var(--accent-2); }
   .lyr-opt .body { flex: 1; min-width: 0; }
   .lyr-opt .body .txt { word-wrap: break-word; }
   .lyr-opt .body .who { color: var(--text-mute); font-size: 11px; margin-top: 2px; }
-  .lyr-opt .act { flex: none; color: var(--text-mute); font-size: 11px; opacity: 0; }
-  .lyr-opt:hover .act { opacity: 1; }
-  .lyr-opt .act:hover { color: var(--accent); }
-  /* A tweak of someone else's suggestion sits under it, one level only. */
-  .lyr-opt.reply { margin-left: 26px; }
-  .lyr-opt.note { cursor: default; }
-  .lyr-opt.note .key { border-style: dashed; }
-  .lyr-pop-add { display: flex; gap: 7px; margin-top: 10px; padding-top: 10px; border-top: 1px solid var(--line); }
-  .lyr-pop-add input {
-    flex: 1; background: var(--field); border: 1px solid var(--line); color: var(--text);
-    border-radius: 8px; padding: 7px 10px; font: inherit; font-size: 13px; outline: none;
-  }
-  .lyr-pop-add input:focus { border-color: var(--accent); }
-  .lyr-pop-add .btn { border: none; border-radius: 8px; padding: 7px 14px; font: inherit; font-size: 12px; font-weight: 600; cursor: pointer; background: var(--accent); color: #fff; }
-  .lyr-pop-add .btn.ghost { background: var(--bg-row); color: var(--text-dim); border: 1px solid var(--line); }
-  /* Comment or suggestion is a choice you make as you write it, on any line —
-     a comment says something about the words without offering replacements. */
-  .lyr-line.noted { box-shadow: inset 2px 0 0 var(--accent-2); }
-  .lyr-note-dot { font-size: 10px; opacity: .65; margin-left: 6px; vertical-align: 1px; }
-  body:not(.lyr-review) .lyr-note-dot { display: none; }
+  .lyr-pop textarea {
+    width: 100%; min-height: 62px; resize: vertical; background: var(--field);
+    border: 1px solid var(--line); color: var(--text); border-radius: 8px;
+    padding: 8px 10px; font: inherit; font-size: 13.5px; outline: none;
+  }
+  .lyr-pop textarea:focus { border-color: var(--accent); }
+  .lyr-pop-foot { display: flex; align-items: center; gap: 8px; margin-top: 8px; }
+  .lyr-assign { display: none; align-items: center; gap: 5px; color: var(--text-dim); font-size: 11.5px; cursor: pointer; }
+  .lyr-assign.on { display: inline-flex; }
+  .lyr-assign b { color: var(--warn); }
+  .lyr-pop-foot .btn { border: none; border-radius: 8px; padding: 7px 14px; font: inherit; font-size: 12px; font-weight: 600; cursor: pointer; background: var(--accent); color: #fff; }
+  .lyr-pop-foot .btn.ghost { background: var(--bg-row); color: var(--text-dim); border: 1px solid var(--line); }
+  /* @ a bandmate. Names, not accounts — there is nothing here to email. */
+  .lyr-ments { display: none; position: absolute; z-index: 2; background: var(--bg); border: 1px solid var(--accent); border-radius: 8px; padding: 4px; min-width: 140px; box-shadow: var(--shadow); }
+  .lyr-ments.open { display: block; }
+  .lyr-ments div { padding: 5px 8px; border-radius: 6px; cursor: pointer; font-size: 12.5px; }
+  .lyr-ments div:hover, .lyr-ments div.on { background: var(--bg-hover); }
+  .lyr-ment { color: var(--accent); font-weight: 600; }
+
+  @media (max-width: 860px) {
+    .lyr-doc { grid-template-columns: 1fr; max-height: 52vh; }
+    .lyr-cards { min-height: 0 !important; }
+    .lyr-card { position: static; margin-bottom: 8px; }
+  }
 
   /* Cross-page buttons + admin: band members only, and never on a shared mix */
   #artBtn, #homeBtn { display: none; }
@@ -595,14 +728,27 @@
     </div>
   </div>
 
-  <!-- A line's alternatives, and the tweaks people hung off them -->
+  <!-- Highlight words and these appear over them -->
+  <div class="lyr-seltool" id="lyrSelTool">
+    <button data-act="comment" title="Comment on the highlighted words (⌘⌥M)">💬 Comment</button>
+    <button data-act="suggest" title="Propose different words">✎ Suggest</button>
+  </div>
+
+  <!-- One box for writing a comment or a suggestion, plus the lines a draft
+       dropped, which can be put back from here -->
   <div class="lyr-pop" id="lyrPop">
     <div class="lyr-pop-head" id="lyrPopHead"></div>
+    <div class="lyr-pop-quote" id="lyrPopQuote"></div>
     <div class="lyr-pop-list" id="lyrPopList"></div>
-    <div class="lyr-pop-add">
-      <input type="text" id="lyrPopInput" maxlength="300" placeholder="Suggest this line instead…" />
-      <button class="btn primary" id="lyrPopGo">Suggest</button>
-      <button class="btn ghost" id="lyrPopNote" title="Say something about this line without replacing it">Comment</button>
+    <textarea id="lyrPopInput" maxlength="600" placeholder="Say something…"></textarea>
+    <div class="lyr-ments" id="lyrMents"></div>
+    <div class="lyr-pop-foot">
+      <label class="lyr-assign" id="lyrAssign" title="Make this their job. Nobody is emailed — it is a name on the card.">
+        <input type="checkbox" id="lyrAssignBox" /> Assign to <b id="lyrAssignWho"></b>
+      </label>
+      <span style="flex:1"></span>
+      <button class="btn ghost" id="lyrPopCancel">Cancel</button>
+      <button class="btn" id="lyrPopGo">Comment</button>
     </div>
   </div>
 
@@ -640,14 +786,27 @@
             </button>
             <div class="vermenu" id="lyrDraftMenu"></div>
           </div>
-          <label class="lyr-switch" title="Suggestions are hidden until you ask for them">
-            <input type="checkbox" id="lyrReview" /> <span>Suggestions <b id="lyrSugCount">0</b></span>
-          </label>
+          <!-- What typing means. Reading is the default: lyrics read as lyrics
+               until somebody goes looking. -->
+          <div class="lyr-modes" id="lyrModes">
+            <button data-mode="read" class="on" title="Just the words. Highlight any of them to comment.">👁 Reading</button>
+            <button data-mode="suggest" title="Tracked changes — what you type is proposed, not applied">✎ Suggesting</button>
+            <button data-mode="edit" title="Rewrite the whole thing. Saving writes a new draft.">✐ Editing</button>
+          </div>
+          <span class="lyr-count" id="lyrSugCount"></span>
           <span style="flex:1"></span>
-          <button class="cdl" id="lyrEdit" title="Write or rewrite these lyrics">✎ edit</button>
         </div>
 
-        <div class="lyr-body" id="lyrBody"></div>
+        <div class="lyr-doc" id="lyrDoc">
+          <div class="lyr-text" id="lyrBody"></div>
+          <div class="lyr-rail" id="lyrRail">
+            <div class="lyr-railtabs" id="lyrRailTabs">
+              <button data-tab="open" class="on">Open</button>
+              <button data-tab="history" title="Resolved comments and rejected suggestions are kept, not deleted">History</button>
+            </div>
+            <div class="lyr-cards" id="lyrCards"></div>
+          </div>
+        </div>
         <textarea class="lyr-edit" id="lyrEditor" spellcheck="false"
                   placeholder="Type the lyrics. One line per line."></textarea>
 
